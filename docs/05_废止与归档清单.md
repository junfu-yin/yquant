# 05 · 废止与归档清单（v3.1 仓库整备指令）

> 目的：让仓库内**只有一套有效真相**。执行人：委托人或其 AI 助手；执行后在本文件勾选并提交。

## 1. 文档处置表

| 旧文件 | 处置 | 说明 |
|---|---|---|
| 量化交易-AI系统_技术方案.md（v1） | → `docs/archive/` | 已废弃历史 |
| 旧 03（v2.x） | → `docs/archive/03_v2.x.md` | 被本包 03 v3.1 取代 |
| 旧 04、旧 10（SOW 两份） | → archive | 合并为 04 v3.1 |
| 旧 05（阶段二 README） | → archive | 内容并入 00/03 |
| 11（v2.2 评审报告） | → archive | 历史评审记录，其未勾销项已在 v3.1 中裁决（R1/R4/R5→已并入；R2/R3/R6/R7 港股与税务项→随 ADR-19/20 废止） |
| 12、13（提案） | → archive | 已正式并入 03 |
| 01、02 | **原位保留** | 过程档案，不回改；02 的当前状态索引见 02A |
| 06/07/08/09 | 用本包 v3.1 版本**覆盖** | 旧版进 archive |

## 2. 内容删除清单（全库生效）
- A 股优先内容：T+1 当日禁售、涨跌停撮合、印花税/过户费默认值、AkShare 主源表述、巨潮/披露易、沪深 300 基准、¥ 金额（模拟盘 ¥300,000 → $50,000）
- 港股适配全部内容（board lot、多币种、VCM/CAS、HKEXnews PDF）——含 icebox 中的"港股"条目降为一句话
- 税务内容：预扣税讨论、税率参数钩子——全库仅保留一句范围声明（03 §2.1）
- LLM 绝对禁令表述（"LLM 不得产生任何信号"）→ 统一为 ADR-22 表述
- 反向 ETF 作为对冲表达的段落（13 篇旧 §8）→ 替换为"防御三件套：现金/久期/黄金"
- 3x ETF 示例（UPRO/TQQQ/TMF/SQQQ/SH/SDS）从可交易域移入 icebox 附注

## 3. 仓库残留自查 v2（ADR-38：白名单+豁免登记）

**范围原则**：只检查**规范性文档集** `SPEC = 03 04 06 07 08 09`。以下文件**不在检查范围**，因为它们合法承载历史语境：00（版本沿革需提及被删概念）、01/02（过程档案，不回改）、02A（决策生死簿，死亡决策必须留名）、05（本清单自身列出违禁词）、`archive/` 全部。这解决了 v1 版 grep 规则"命中档案与自身"的自相矛盾。

```bash
cd docs && SPEC="03*.md 04*.md 06*.md 07*.md 08*.md 09*.md"
# G1 A股交易规则残留（期望 0 命中）
grep -n "涨跌停\|当日买入不可卖\|印花税\|过户费\|巨潮" $SPEC
# G2 A股基准/数据源残留（期望 0）
grep -n "沪深300\|AkShare" $SPEC
# G3 港股实现残留（排除"非目标/不做"声明行；期望 0）
grep -n "港股\|HKEX\|board lot\|每手" $SPEC | grep -v "不做\|非目标\|icebox"
# G4 税务实现残留（排除范围声明；期望 0）
grep -n "预扣\|withholding\|税率\|tax_" $SPEC
# G5 禁用杠杆工具 ticker（期望 0；SSO/QLD/UGL 为合法 2x 不在列）
grep -n "SQQQ\|TQQQ\|UPRO\|TMF\|SDS\|SPXU" $SPEC
# G6 币种残留（期望 0）
grep -n "¥\|人民币\|CNY" $SPEC
```

**豁免登记表**：若某命中经人工判定为合法表述（如教学性反例），不修改原文，改为在下表登记，登记行视为通过：

| 规则 | 文件:行 | 命中文本 | 豁免理由 | 登记人/日期 |
|---|---|---|---|---|
| G3 | 03 版本变更表 v3.1 行 | "删除 A 股/港股/税务/LLM 绝对禁令冲突内容" | changelog 记录被删概念属合法历史表述，不构成实现残留 | Claude / 2026-07-05 |

## 4. 提交建议
单独一个 commit：`docs: v3.1 全量升级——全球宏观×美股执行正式版（并入12/13，废止A股/港股/税务/绝对禁令冲突项）`，并在 commit message 中引用本清单编号。
